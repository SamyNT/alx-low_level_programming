#include "main.h"
/**
 *_isdigit - Function check if the input is a  letter
 *@c: - number to compare
 * Return: 1 if true 0 if false
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 58)
	{
		return (1);
	}
	else
	{
	return (0);
	}
}

